#include <stdio.h>

/**
 * main - this is a function that executes any code within its curly braces
 *
 * Return:always 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
